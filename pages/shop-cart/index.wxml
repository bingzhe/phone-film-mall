<!--index.wxml-->
<import src="../template-cart/template-cart.wxml"></import>

<view class="container">
  <template
    is="{{ goodsList.length > 0 ? 'cart-goods-list' : 'no-goods' }}"
    data="{{ saveHidden, allSelect, noSelect,shopCarType,goodsList,totalPrice}}"
  ></template>
</view>
